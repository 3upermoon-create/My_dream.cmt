services:
  - type: web
    name: bet-analyst
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: API_FOOTBALL_KEY
        sync: false
